<h2>Listing logs</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for log <- @logs do %>
    <tr>
      <td><%= log.name %></td>

      <td class="text-right">
        <%= link "Show", to: log_path(@conn, :show, log), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: log_path(@conn, :edit, log), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: log_path(@conn, :delete, log), method: :delete, class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New log", to: log_path(@conn, :new) %>
